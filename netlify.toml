[build]
  command = "npm install"
  functions = "netlify/functions"
  publish = "public"

[dev]
  framework = "#custom"
  command = "npx netlify-cli dev"
  port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
